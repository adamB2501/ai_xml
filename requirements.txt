# Pinned to what this project actually imports, for the exact machine this
# was developed on. This REPLACES the old requirements.txt, which was a
# raw `pip freeze` of the whole dev venv (248 packages, most unrelated --
# leftovers from spaCy/docling/marker experiments that are no longer part
# of the pipeline). Install with:
#
#   pip install -r requirements.txt
#
# (or `pip install .` / `pip install .[dev]`, which reads the same set
# from pyproject.toml -- this file exists as a locked, copy-pasteable
# version pin, e.g. for setting up a second machine identically.)

# --- core -------------------------------------------------------------
pdfplumber==0.11.10
pydantic==2.13.4
Pillow==10.4.0
fastapi==0.141.1
uvicorn==0.52.4

# --- dev / tests --------------------------------------------------------
# Only needed if you're running the test suite, not for using the pipeline.
pytest==9.1.1
httpx==0.28.1

# --- NOT pip-installable: separate runtime dependency ------------------
# Ollama (https://ollama.com) must be installed and running separately --
# it's how every model call in accurate_pipe/llm.py reaches the local
# HTTP API (stdlib urllib, no client library needed). After installing:
#
#   ollama pull qwen2.5vl:7b     # vision model -- scanned pages, rasterized
#                                 # letterhead/footer bands
#   ollama pull qwen3:14b        # text extraction model
#                                 # (adaptive_pipe.config.TEXT_MODEL)
#
# On a lower-VRAM machine, see README.md's hardware note before assuming
# these are the right model sizes to pull.

# --- also NOT pip-installable: standard library, but not always bundled -
# tkinter is used by adaptive_pipe/ui/server.py for the native file/folder
# picker dialogs. It ships with the standard python.org Windows/macOS
# installers; on Linux it's typically a separate OS package
# (e.g. `apt install python3-tk`), not something pip can add.
